# Supported environment: Ubuntu 24.04 LTS with Python 3.12.
# ROS2 Jazzy is installed with apt or through docker/Dockerfile, not pip.

setuptools>=68
mujoco>=3.2
numpy>=1.26
imageio>=2.34
imageio-ffmpeg>=0.4.9
PyYAML>=6.0

# Local VLM/VLA runtime dependencies. Model weights are downloaded separately
# into the ignored models/ directory by scripts/download_models.py.
torch>=2.4
transformers>=4.45
Pillow>=10.0
huggingface-hub>=0.24
python-dotenv>=1.0
lerobot

# Optional cloud planner:
# anthropic>=0.40
