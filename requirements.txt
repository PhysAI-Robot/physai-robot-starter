# Phase 0 — simulation only. No ROS2, no torch required to run the sim.
setuptools>=68
mujoco>=3.2
numpy>=1.26
imageio>=2.34
imageio-ffmpeg>=0.4.9
PyYAML>=6.0

# Optional model dependencies live in pyproject.toml extras:
# python -m pip install -e ".[smolvlm]"  # local SmolVLM planner
# python -m pip install -e ".[vlm]"      # Claude planner
# python -m pip install -e ".[vla]"      # LeRobot VLA policies
